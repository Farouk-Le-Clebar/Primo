import UsersList from "./components/UsersList";

const AdminPanel = () => {

    return (
        <div className="w-full h-full p-10">
            <UsersList />
        </div>
    );
};

export default AdminPanel