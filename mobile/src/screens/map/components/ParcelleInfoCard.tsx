import { View } from 'react-native';
import Localisation from './widgets/Localisation';

type ParcelleInfoCardProps = {

};

const ParcelleInfoCard = () => {
    return (
        <View>
            <Localisation />
        </View>
    );
}

export default ParcelleInfoCard;